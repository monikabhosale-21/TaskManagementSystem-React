import React from "react";

const AdminDashboard = () => {
  return (
    <div>
      <h2>Welcome Admin</h2>
      {/* Admin-only components like AddEmployee, CreateTask, etc. */}
    </div>
  );
};

export default AdminDashboard;
