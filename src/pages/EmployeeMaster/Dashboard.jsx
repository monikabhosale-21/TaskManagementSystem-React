import React from "react";
import EmployeeLayout from "../../layouts/EmployeeLayout";

const EmployeeDashboard = () => {
  return (
    <EmployeeLayout>
      <h2>Welcome to Employee Dashboard</h2>
      {/* Show assigned tasks, deadlines, etc. */}
    </EmployeeLayout>
  );
};

export default EmployeeDashboard;
